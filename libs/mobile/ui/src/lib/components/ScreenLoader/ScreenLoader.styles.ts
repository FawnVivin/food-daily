import styled from "styled-components";
import { View } from "react-native";

export const ScreenLoaderRoot = styled(View)`
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1;
`
