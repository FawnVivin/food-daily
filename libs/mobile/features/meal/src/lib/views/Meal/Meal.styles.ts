import styled from 'styled-components'
import { View } from 'react-native'

export const MealWrapper = styled(View)`
  padding: 0 25px;
`
export const PlusButtonWrapper = styled(View)`
  position: absolute;
  left: 15px;
  bottom:15px;
`
