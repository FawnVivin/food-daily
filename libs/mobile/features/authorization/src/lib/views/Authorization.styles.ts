import styled from 'styled-components'
import { View } from 'react-native'

export const AuthorizationRoot = styled(View)`
  flex: 1;
  justify-content: center;
  gap: 25px;
  padding: 0 36px;
  width: 100%;
`

export const Title = styled(View)`
  gap: 5px;
  align-items: center;
`
