import styled from 'styled-components'
import { View } from 'react-native'

export const TrainerInfoRoot = styled(View)`
  flex-direction: row;
  gap: 10px;
  align-items: center;
  margin-bottom: 15px;
`

export const TextWrapper = styled(View)`
  display: flex;
  gap: 5px;
  flex: 1;
`