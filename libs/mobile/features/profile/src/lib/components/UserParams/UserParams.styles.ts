import styled from 'styled-components'
import { View } from 'react-native'

export const UserParamsRoot = styled(View)`
  flex-direction: row;
  align-items: center;
  justify-content: space-around;
`
