import styled from 'styled-components'
import { View } from 'react-native'

export const UserInfoRoot = styled(View)`
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
`

export const MainInfoWrapper = styled(View)`
  flex-direction: row;
  gap:10px;
  align-items: center;
`
