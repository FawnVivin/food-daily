import styled from 'styled-components'
import { View } from 'react-native'

export const ProfileFormRoot = styled(View)`
  gap: 10px;
  flex:1;
`

export const ButtonWrapper = styled(View)`
  padding: 0 0 10px 0;
`
