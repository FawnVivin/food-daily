import styled from 'styled-components'
import { View } from 'react-native'

export const UserProductRoot = styled(View)`
  flex: 1;
`

export const ButtonWrapper = styled(View)`
  padding: 10px 36px;
`
