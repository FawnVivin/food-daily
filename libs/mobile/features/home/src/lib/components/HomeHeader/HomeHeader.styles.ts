import styled from 'styled-components'
import {Text} from 'react-native-paper'
import { View } from 'react-native'

export const HomeHeaderRoot = styled(View)`
  padding-top: 20px;
`

export const UserName = styled(Text)`
  font-size: 20px;
  font-weight: 700;
  line-height: 30px
`
