//import { Column, CreateDateColumn, Entity, JoinColumn, ManyToOne, OneToMany, PrimaryGeneratedColumn } from "typeorm";
//import { Role } from "@food-daily/shared/types";
//import { ConsumedProduct, Product, User } from "@food-daily/api/models";

//import type { Activity, Sex, Target, User as UserType } from "@food-daily/shared/types";

//@Entity()
//export class Weight implements WeightType {
  //@PrimaryGeneratedColumn()
  //id: number;
  //@Column()
  //quantity: string;
  //@CreateDateColumn()
  //date: Date;
  //@ManyToOne(() => User, (user) => user.weight)
  //@JoinColumn({ name: "userId" })
  //userId: number;
  //constructor(weight: Partial<Weight>) {
   // Object.assign(this, weight);
  //}
//}
